# API Configuration
# Leave VITE_API_URL empty to use the Vite dev server proxy (recommended for development)
# The proxy is configured in vite.config.js to forward /api requests to http://localhost:3900
VITE_API_URL=

# For production, set this to your actual API URL:
# VITE_API_URL=https://your-api-domain.com
